app_code = r'''import streamlit as st
import pandas as pd
import numpy as np
import threading
import time as time_module
import sys

# ============================================
# SIX STRING BOOT CAMP - Complete App
# ============================================

try:
    st.set_page_config(
        page_title="Six String Boot Camp",
        page_icon="🎸",
        layout="wide",
        initial_sidebar_state="collapsed"
    )
except Exception:
    pass

# ============================================
# CUSTOM CSS
# ============================================
st.markdown("""
<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap');

    html, body, [class*="css"] {
        font-family: 'Inter', sans-serif;
    }

    .main-header {
        text-align: center;
        padding: 2.5rem 0;
        background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
        border-radius: 20px;
        margin-bottom: 2rem;
        border: 1px solid rgba(233, 69, 96, 0.2);
    }

    .main-header h1 {
        color: #e94560;
        font-size: 3.2rem;
        font-weight: 700;
        margin: 0;
        letter-spacing: -2px;
        text-shadow: 0 0 30px rgba(233, 69, 96, 0.3);
    }

    .main-header p {
        color: #a0a0a0;
        font-size: 1.15rem;
        margin-top: 0.5rem;
        font-weight: 300;
    }

    .tool-card {
        background: linear-gradient(145deg, #16213e, #0f3460);
        border-radius: 16px;
        padding: 1.5rem;
        border: 1px solid rgba(233, 69, 96, 0.1);
        transition: all 0.3s ease;
        height: 100%;
    }

    .tool-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 12px 32px rgba(233, 69, 96, 0.2);
        border-color: rgba(233, 69, 96, 0.3);
    }

    .stButton>button {
        background: linear-gradient(90deg, #e94560, #ff6b6b) !important;
        color: white !important;
        border: none !important;
        border-radius: 10px !important;
        padding: 0.75rem 2rem !important;
        font-weight: 600 !important;
        font-size: 0.95rem !important;
        transition: all 0.3s ease !important;
        width: 100% !important;
    }

    .stButton>button:hover {
        transform: scale(1.03) !important;
        box-shadow: 0 6px 20px rgba(233, 69, 96, 0.4) !important;
    }

    .lesson-card {
        background: linear-gradient(145deg, #16213e, #1a1a2e);
        border-left: 4px solid #e94560;
        border-radius: 0 14px 14px 0;
        padding: 1.5rem;
        margin: 1rem 0;
        transition: all 0.3s ease;
    }

    .lesson-card:hover {
        background: linear-gradient(145deg, #1a1a2e, #16213e);
        border-left-width: 6px;
    }

    .lesson-card.free {
        border-left-color: #4ade80;
    }

    .song-card {
        background: linear-gradient(145deg, #16213e, #0f3460);
        border-radius: 14px;
        padding: 1.25rem;
        border: 1px solid rgba(15, 52, 96, 0.5);
        transition: all 0.3s ease;
        height: 100%;
    }

    .song-card:hover {
        border-color: rgba(233, 69, 96, 0.3);
        transform: translateY(-4px);
    }

    .badge {
        display: inline-block;
        padding: 0.2rem 0.6rem;
        border-radius: 6px;
        font-size: 0.75rem;
        font-weight: 600;
        margin-right: 0.4rem;
    }

    .badge-beginner { background: rgba(74, 222, 128, 0.15); color: #4ade80; }
    .badge-intermediate { background: rgba(96, 165, 250, 0.15); color: #60a5fa; }
    .badge-advanced { background: rgba(233, 69, 96, 0.15); color: #e94560; }
    .badge-rock { background: rgba(139, 92, 246, 0.15); color: #a78bfa; }
    .badge-pop { background: rgba(236, 72, 153, 0.15); color: #f472b6; }
    .badge-folk { background: rgba(34, 197, 94, 0.15); color: #4ade80; }

    .fretboard-container {
        background: #1a1a2e;
        border-radius: 12px;
        padding: 1rem;
        overflow-x: auto;
    }

    .progress-week {
        display: flex;
        align-items: center;
        padding: 0.6rem 1rem;
        border-radius: 10px;
        margin-bottom: 0.3rem;
        transition: all 0.2s ease;
    }

    .progress-week:hover {
        background: rgba(233, 69, 96, 0.05);
    }

    .sergeant-box {
        background: linear-gradient(145deg, #16213e, #1a1a2e);
        border-radius: 20px;
        padding: 2.5rem;
        border: 2px solid rgba(233, 69, 96, 0.3);
        position: relative;
        overflow: hidden;
    }

    .metric-card {
        background: linear-gradient(145deg, #16213e, #0f3460);
        border-radius: 14px;
        padding: 1.25rem;
        text-align: center;
        border: 1px solid rgba(233, 69, 96, 0.1);
    }

    .upsell-box {
        background: linear-gradient(145deg, #16213e, #1a1a2e);
        border-radius: 20px;
        padding: 2.5rem;
        text-align: center;
        border: 1px solid rgba(233, 69, 96, 0.2);
        position: relative;
    }
</style>
""", unsafe_allow_html=True)

# ============================================
# SESSION STATE
# ============================================
def init_session_state():
    defaults = {
        'user_progress': {'lessons_completed': [], 'current_lesson': 0, 'streak_days': 0, 'total_practice_minutes': 0},
        'sergeant_intro_seen': False,
        'metronome_running': False,
        'metronome_bpm': 120
    }
    for key, value in defaults.items():
        if key not in st.session_state:
            st.session_state[key] = value

init_session_state()

# ============================================
# SVG FRETBOARD RENDERER
# ============================================
def render_fretboard(highlight_notes=None, title=""):
    if highlight_notes is None:
        highlight_notes = []

    width, height = 900, 160
    fret_count = 12
    string_count = 6
    bg_color = "#1a1a2e"
    fret_color = "#444"
    string_color = "#c0a060"
    highlight_color = "#e94560"
    nut_color = "#f5f5dc"

    svg = f'<svg width="100%" height="{height}" viewBox="0 0 {width} {height}" style="background: {bg_color}; border-radius: 12px;">'

    fret_spacing = width / (fret_count + 1)

    for i in range(fret_count + 1):
        x = i * fret_spacing
        color = nut_color if i == 0 else fret_color
        width_line = 5 if i == 0 else 1.5
        svg += f'<line x1="{x}" y1="12" x2="{x}" y2="{height-12}" stroke="{color}" stroke-width="{width_line}"/>'

    string_spacing = (height - 24) / (string_count - 1)
    for i in range(string_count):
        y = 12 + i * string_spacing
        thickness = max(1.5, 3.5 - (i * 0.4))
        svg += f'<line x1="0" y1="{y}" x2="{width}" y2="{y}" stroke="{string_color}" stroke-width="{thickness}"/>'

    marker_frets = [3, 5, 7, 9, 12]
    for fret in marker_frets:
        x = (fret - 0.5) * fret_spacing
        if fret == 12:
            svg += f'<circle cx="{x}" cy="{height/2 - 18}" r="7" fill="#555"/>'
            svg += f'<circle cx="{x}" cy="{height/2 + 18}" r="7" fill="#555"/>'
        else:
            svg += f'<circle cx="{x}" cy="{height/2}" r="7" fill="#555"/>'

    for i in range(1, fret_count + 1):
        x = (i - 0.5) * fret_spacing
        svg += f'<text x="{x}" y="{height - 2}" text-anchor="middle" fill="#666" font-size="11" font-family="Inter, sans-serif">{i}</text>'

    for note_info in highlight_notes:
        string_idx = note_info.get("string", 0)
        fret = note_info.get("fret", 0)
        note_name = note_info.get("note", "")
        finger = note_info.get("finger", "")

        x = (fret + 0.5) * fret_spacing if fret > 0 else 8
        y = 12 + string_idx * string_spacing

        if fret == 0:
            svg += f'<circle cx="{x}" cy="{y}" r="11" fill="none" stroke="{highlight_color}" stroke-width="2.5"/>'
            svg += f'<text x="{x}" y="{y+4}" text-anchor="middle" fill="{highlight_color}" font-size="10" font-weight="bold" font-family="Inter, sans-serif">{note_name}</text>'
        elif fret == -1:
            svg += f'<text x="{x}" y="{y+5}" text-anchor="middle" fill="#888" font-size="14" font-weight="bold" font-family="Inter, sans-serif">✕</text>'
        else:
            svg += f'<circle cx="{x}" cy="{y}" r="13" fill="{highlight_color}" opacity="0.95"/>'
            svg += f'<text x="{x}" y="{y+4}" text-anchor="middle" fill="white" font-size="10" font-weight="bold" font-family="Inter, sans-serif">{note_name}</text>'
            if finger:
                svg += f'<text x="{x}" y="{y+22}" text-anchor="middle" fill="#e94560" font-size="9" font-family="Inter, sans-serif">{finger}</text>'

    string_labels = ["e", "B", "G", "D", "A", "E"]
    for i, label in enumerate(string_labels):
        y = 12 + i * string_spacing
        svg += f'<text x="8" y="{y+4}" text-anchor="middle" fill="#888" font-size="11" font-weight="600" font-family="Inter, sans-serif">{label}</text>'

    svg += '</svg>'

    if title:
        return f'<div class="fretboard-container"><p style="color: #e94560; font-weight: 600; margin-bottom: 0.5rem; font-size: 1.1rem;">{title}</p>{svg}</div>'
    return f'<div class="fretboard-container">{svg}</div>'

# ============================================
# DATA
# ============================================
CHORD_SHAPES = {
    "C Major": {
        "ascii": "e|--0--|\nB|--1--|\nG|--0--|\nD|--2--|\nA|--3--|\nE|--x--|",
        "svg_notes": [
            {"string": 0, "fret": 0, "note": "E"},
            {"string": 1, "fret": 1, "note": "C", "finger": "1"},
            {"string": 2, "fret": 0, "note": "G"},
            {"string": 3, "fret": 2, "note": "E", "finger": "2"},
            {"string": 4, "fret": 3, "note": "C", "finger": "3"},
            {"string": 5, "fret": -1, "note": "X"}
        ]
    },
    "G Major": {
        "ascii": "e|--3--|\nB|--0--|\nG|--0--|\nD|--0--|\nA|--2--|\nE|--3--|",
        "svg_notes": [
            {"string": 0, "fret": 3, "note": "G", "finger": "4"},
            {"string": 1, "fret": 0, "note": "B"},
            {"string": 2, "fret": 0, "note": "G"},
            {"string": 3, "fret": 0, "note": "D"},
            {"string": 4, "fret": 2, "note": "B", "finger": "2"},
            {"string": 5, "fret": 3, "note": "G", "finger": "3"}
        ]
    },
    "D Major": {
        "ascii": "e|--2--|\nB|--3--|\nG|--2--|\nD|--0--|\nA|--x--|\nE|--x--|",
        "svg_notes": [
            {"string": 0, "fret": 2, "note": "F#", "finger": "2"},
            {"string": 1, "fret": 3, "note": "D", "finger": "3"},
            {"string": 2, "fret": 2, "note": "A", "finger": "1"},
            {"string": 3, "fret": 0, "note": "D"},
            {"string": 4, "fret": -1, "note": "X"},
            {"string": 5, "fret": -1, "note": "X"}
        ]
    },
    "A Major": {
        "ascii": "e|--0--|\nB|--2--|\nG|--2--|\nD|--2--|\nA|--0--|\nE|--x--|",
        "svg_notes": [
            {"string": 0, "fret": 0, "note": "E"},
            {"string": 1, "fret": 2, "note": "C#", "finger": "2"},
            {"string": 2, "fret": 2, "note": "A", "finger": "3"},
            {"string": 3, "fret": 2, "note": "E", "finger": "1"},
            {"string": 4, "fret": 0, "note": "A"},
            {"string": 5, "fret": -1, "note": "X"}
        ]
    },
    "E Major": {
        "ascii": "e|--0--|\nB|--0--|\nG|--1--|\nD|--2--|\nA|--2--|\nE|--0--|",
        "svg_notes": [
            {"string": 0, "fret": 0, "note": "E"},
            {"string": 1, "fret": 0, "note": "B"},
            {"string": 2, "fret": 1, "note": "G#", "finger": "1"},
            {"string": 3, "fret": 2, "note": "E", "finger": "2"},
            {"string": 4, "fret": 2, "note": "B", "finger": "3"},
            {"string": 5, "fret": 0, "note": "E"}
        ]
    },
    "Am": {
        "ascii": "e|--0--|\nB|--1--|\nG|--2--|\nD|--2--|\nA|--0--|\nE|--x--|",
        "svg_notes": [
            {"string": 0, "fret": 0, "note": "E"},
            {"string": 1, "fret": 1, "note": "C", "finger": "1"},
            {"string": 2, "fret": 2, "note": "A", "finger": "2"},
            {"string": 3, "fret": 2, "note": "E", "finger": "3"},
            {"string": 4, "fret": 0, "note": "A"},
            {"string": 5, "fret": -1, "note": "X"}
        ]
    },
    "Em": {
        "ascii": "e|--0--|\nB|--0--|\nG|--0--|\nD|--2--|\nA|--2--|\nE|--0--|",
        "svg_notes": [
            {"string": 0, "fret": 0, "note": "E"},
            {"string": 1, "fret": 0, "note": "B"},
            {"string": 2, "fret": 0, "note": "G"},
            {"string": 3, "fret": 2, "note": "E", "finger": "2"},
            {"string": 4, "fret": 2, "note": "B", "finger": "3"},
            {"string": 5, "fret": 0, "note": "E"}
        ]
    },
    "Dm": {
        "ascii": "e|--1--|\nB|--3--|\nG|--2--|\nD|--0--|\nA|--x--|\nE|--x--|",
        "svg_notes": [
            {"string": 0, "fret": 1, "note": "F", "finger": "1"},
            {"string": 1, "fret": 3, "note": "D", "finger": "3"},
            {"string": 2, "fret": 2, "note": "A", "finger": "2"},
            {"string": 3, "fret": 0, "note": "D"},
            {"string": 4, "fret": -1, "note": "X"},
            {"string": 5, "fret": -1, "note": "X"}
        ]
    },
    "F Major": {
        "ascii": "e|--1--|\nB|--1--|\nG|--2--|\nD|--3--|\nA|--3--|\nE|--1--|",
        "svg_notes": [
            {"string": 0, "fret": 1, "note": "F", "finger": "1"},
            {"string": 1, "fret": 1, "note": "C", "finger": "1"},
            {"string": 2, "fret": 2, "note": "A", "finger": "2"},
            {"string": 3, "fret": 3, "note": "F", "finger": "3"},
            {"string": 4, "fret": 3, "note": "C", "finger": "4"},
            {"string": 5, "fret": 1, "note": "F", "finger": "1"}
        ]
    },
    "B7": {
        "ascii": "e|--2--|\nB|--0--|\nG|--2--|\nD|--1--|\nA|--2--|\nE|--x--|",
        "svg_notes": [
            {"string": 0, "fret": 2, "note": "F#", "finger": "3"},
            {"string": 1, "fret": 0, "note": "B"},
            {"string": 2, "fret": 2, "note": "A", "finger": "2"},
            {"string": 3, "fret": 1, "note": "D#", "finger": "1"},
            {"string": 4, "fret": 2, "note": "B", "finger": "4"},
            {"string": 5, "fret": -1, "note": "X"}
        ]
    }
}

SONGS = [
    {"title": "Wonderwall", "artist": "Oasis", "difficulty": "Beginner", "genre": "Rock", "chords": "Em, G, D, A", "chord_count": 4},
    {"title": "Hotel California", "artist": "Eagles", "difficulty": "Advanced", "genre": "Rock", "chords": "Am, E7, G, D, F", "chord_count": 5},
    {"title": "Knockin' on Heaven's Door", "artist": "Bob Dylan", "difficulty": "Beginner", "genre": "Folk", "chords": "G, D, Am, C", "chord_count": 4},
    {"title": "Stairway to Heaven", "artist": "Led Zeppelin", "difficulty": "Advanced", "genre": "Rock", "chords": "Am, C, D, F, G", "chord_count": 5},
    {"title": "Horse with No Name", "artist": "America", "difficulty": "Beginner", "genre": "Folk", "chords": "Em, D6/F#", "chord_count": 2},
    {"title": "Blackbird", "artist": "The Beatles", "difficulty": "Intermediate", "genre": "Rock", "chords": "G, Am, G/B, C", "chord_count": 4},
    {"title": "Smoke on the Water", "artist": "Deep Purple", "difficulty": "Beginner", "genre": "Rock", "chords": "G, Bb, C", "chord_count": 3},
    {"title": "Dust in the Wind", "artist": "Kansas", "difficulty": "Intermediate", "genre": "Rock", "chords": "C, G, Am, G/B", "chord_count": 4},
    {"title": "House of the Rising Sun", "artist": "The Animals", "difficulty": "Beginner", "genre": "Folk", "chords": "Am, C, D, F, E", "chord_count": 5},
    {"title": "Sweet Child O' Mine", "artist": "Guns N' Roses", "difficulty": "Advanced", "genre": "Rock", "chords": "D, C, G", "chord_count": 3},
    {"title": "Let It Be", "artist": "The Beatles", "difficulty": "Beginner", "genre": "Pop", "chords": "C, G, Am, F", "chord_count": 4},
    {"title": "Wish You Were Here", "artist": "Pink Floyd", "difficulty": "Intermediate", "genre": "Rock", "chords": "G, Em, A, C", "chord_count": 4}
]

CURRICULUM = [
    {"week": 1, "title": "Finger Exercises & Independence", "free": True,
     "desc": "Build dexterity with spider crawls, finger lifts, and stretch routines. Develop finger strength and coordination.",
     "duration": "15 min", "focus": "All four fingers"},
    {"week": 2, "title": "Fretboard Familiarization", "free": True,
     "desc": "Learn every note on the fretboard using patterns and memory anchors. No more guessing where notes are.",
     "duration": "20 min", "focus": "Natural notes"},
    {"week": 3, "title": "Open Chords Mastery", "free": True,
     "desc": "C, G, D, A, E, Am, Em, Dm — clean transitions, no buzz. The foundation of rhythm guitar.",
     "duration": "20 min", "focus": "8 essential chords"},
    {"week": 4, "title": "Two Movable A-Shape Chords", "free": False,
     "desc": "Unlock the entire neck with barre chord fundamentals. The gateway to playing in any key.",
     "duration": "25 min", "focus": "Barre technique"},
    {"week": 5, "title": "Pentatonic Scale — All 5 Positions", "free": False,
     "desc": "The scale that built rock. Master all five boxes and connect them across the entire fretboard.",
     "duration": "30 min", "focus": "Minor pentatonic"},
    {"week": 6, "title": "RECAP & Integration Week", "free": False,
     "desc": "Solidify weeks 1-5. Play along with backing tracks and apply everything you've learned so far.",
     "duration": "30 min", "focus": "Application"},
    {"week": 7, "title": "Circle of Fifths", "free": False,
     "desc": "Understand key signatures, chord relationships, and how songs are built. Music theory made visual.",
     "duration": "25 min", "focus": "Theory"},
    {"week": 8, "title": "Chromatic Scale & Technique", "free": False,
     "desc": "One-finger-per-fret discipline across all strings. Build speed and accuracy simultaneously.",
     "duration": "25 min", "focus": "Speed & accuracy"},
    {"week": 9, "title": "Strumming Techniques", "free": False,
     "desc": "Patterns, dynamics, ghost notes, and groove. Transform basic chords into musical expression.",
     "duration": "20 min", "focus": "Rhythm"},
    {"week": 10, "title": "Picking Techniques", "free": False,
     "desc": "Alternate picking, economy picking, hybrid picking. Clean articulation at any speed.",
     "duration": "25 min", "focus": "Articulation"},
    {"week": 11, "title": "Tying It All Together", "free": False,
     "desc": "Combine chords, scales, and techniques into musical phrases. Start sounding like a guitarist.",
     "duration": "30 min", "focus": "Musicality"},
    {"week": 12, "title": "The Finale: Graduate Performance", "free": False,
     "desc": "Perform a complete song from start to finish. Earn your Six String Diploma and join the ranks.",
     "duration": "40 min", "focus": "Performance"}
]

# ============================================
# HEADER
# ============================================
st.markdown("""
<div class="main-header">
    <h1>🎸 Six String Boot Camp</h1>
    <p>Master the fretboard. One note at a time.</p>
</div>
""", unsafe_allow_html=True)

# ============================================
# SERGEANT MARTIN
# ============================================
if not st.session_state.sergeant_intro_seen:
    st.markdown("""
    <div class="sergeant-box">
        <h2 style="color: #e94560; margin-top: 0; font-size: 1.5rem;">👨‍✈️ Sergeant Martin Reporting For Duty</h2>
        <p style="color: #e0e0e0; font-size: 1.1rem; line-height: 1.7; margin-bottom: 0;">
        "Listen up, recruit! I'm Sergeant Martin, your AI drill instructor. I'm here to turn you from a bedroom strummer into a fretboard warrior.
        <br><br>
        We'll start with the foundations — finger exercises, open chords, and the pentatonic scale. No shortcuts. No excuses.
        <br><br>
        <strong>Your first mission:</strong> Grab your guitar and head to the <em>Free Tools</em> section. Tune up. Then we'll begin Week 1."
        </p>
    </div>
    """, unsafe_allow_html=True)
    if st.button("🫡 Roger That, Sergeant!", key="dismiss_sergeant", use_container_width=True):
        st.session_state.sergeant_intro_seen = True
        st.rerun()

# ============================================
# NAVIGATION TABS
# ============================================
tab_home, tab_tools, tab_lessons, tab_songs, tab_progress = st.tabs([
    "🏠 Home", "🛠️ Free Tools", "📚 12-Week Course", "🎵 Song Library", "📊 My Progress"
])

# ============================================
# TAB 1: HOME
# ============================================
with tab_home:
    col1, col2 = st.columns([2, 1])

    with col1:
        st.subheader("Welcome, Recruit")
        st.write("""
        **Six String Boot Camp** is your complete guitar mastery system. No fluff. No filler. Just results.

        **Here's how it works:**
        - **Free Forever:** Tuner, metronome, chord library, and song database — genuinely useful from day one
        - **12-Week Foundation:** Structured curriculum that builds real, transferable skill
        - **Visual Fretboard:** See exactly where to place your fingers with interactive diagrams
        - **Sergeant Martin:** Your AI mentor, keeping you accountable and on track

        Start with the free tools. Have fun. Learn a song. When you're ready to level up, the 12-week course is waiting.
        """)

        st.markdown("---")

        c1, c2, c3 = st.columns(3)
        c1.metric("Free Tools", "4", "Always Available")
        c2.metric("Course Lessons", "12", "Week-by-Week")
        c3.metric("Song Library", "500+", "Tabs & Chords")

    with col2:
        st.subheader("Today's Mission")
        st.markdown("""
        <div class="tool-card">
            <h4 style="color: #e94560; margin-top: 0; font-size: 1.1rem;">🎯 Daily Drill</h4>
            <p style="color: #a0a0a0; margin: 0.5rem 0;">Complete your first finger exercise</p>
            <div style="border-top: 1px solid rgba(233,69,96,0.2); margin: 1rem 0; padding-top: 1rem;">
                <p style="color: #e0e0e0; margin: 0.3rem 0;"><strong>⏱️ Time:</strong> 5 minutes</p>
                <p style="color: #e0e0e0; margin: 0.3rem 0;"><strong>🎯 Focus:</strong> Finger independence</p>
                <p style="color: #e0e0e0; margin: 0.3rem 0;"><strong>🎸 Gear:</strong> Any guitar</p>
            </div>
        </div>
        """, unsafe_allow_html=True)

        if st.button("🚀 Start Mission", use_container_width=True):
            st.info("Head to the **Free Tools** tab and fire up the tuner. Then come back for Week 1!")

# ============================================
# TAB 2: FREE TOOLS
# ============================================
with tab_tools:
    st.subheader("🛠️ Free Tools — Always Available")
    st.caption("These tools are 100% free. No account required. No limits. No ads. Ever.")

    tool_col1, tool_col2 = st.columns(2)

    # --- GUITAR TUNER ---
    with tool_col1:
        st.markdown('<div class="tool-card">', unsafe_allow_html=True)
        st.markdown("### 🎚️ Reference Tuner")
        st.write("Standard E-A-D-G-B-E tuning. Select a string and match the pitch.")

        string = st.selectbox("Select String",
                             ["E (Low — 6th)", "A (5th)", "D (4th)", "G (3rd)", "B (2nd)", "E (High — 1st)"],
                             key="tuner_string")

        frequencies = {
            "E (Low — 6th)": 82.41, "A (5th)": 110.00, "D (4th)": 146.83,
            "G (3rd)": 196.00, "B (2nd)": 246.94, "E (High — 1st)": 329.63
        }

        freq = frequencies[string]

        st.markdown(f"""
        <div style="text-align: center; padding: 1.5rem; background: #1a1a2e; border-radius: 12px; margin: 1rem 0; border: 1px solid rgba(233,69,96,0.2);">
            <p style="font-size: 2.5rem; color: #e94560; margin: 0; font-weight: 700;">{string.split(' ')[0]}</p>
            <p style="font-size: 1.4rem; color: #a0a0a0; margin: 0.5rem 0;">{freq} Hz</p>
            <p style="font-size: 0.85rem; color: #666; margin-top: 0.5rem;">Play this note on your guitar and match the pitch</p>
        </div>
        """, unsafe_allow_html=True)

        st.markdown("""
        <div style="background: rgba(74,222,128,0.1); border-radius: 8px; padding: 0.75rem; margin-top: 0.5rem;">
            <p style="color: #4ade80; margin: 0; font-size: 0.85rem;">💡 <strong>Pro tip:</strong> Use a tuning fork or reference tone app for audio. This visual guide shows the target frequency.</p>
        </div>
        """, unsafe_allow_html=True)
        st.markdown('</div>', unsafe_allow_html=True)

    # --- METRONOME ---
    with tool_col2:
        st.markdown('<div class="tool-card">', unsafe_allow_html=True)
        st.markdown("### ⏱️ Metronome")
        st.write("Keep perfect time. Essential for rhythm development.")

        bpm = st.slider("BPM", 40, 240, st.session_state.metronome_bpm, 1, key="metro_bpm_slider")
        st.session_state.metronome_bpm = bpm

        metro_col1, metro_col2 = st.columns([1, 2])

        with metro_col1:
            if st.button("▶️ Start" if not st.session_state.metronome_running else "⏹️ Stop",
                        key="metro_toggle_btn", use_container_width=True):
                st.session_state.metronome_running = not st.session_state.metronome_running
                st.rerun()

        with metro_col2:
            st.markdown(f"""
            <div style="text-align: center; padding: 0.5rem;">
                <p style="color: #e94560; font-size: 1.8rem; font-weight: 700; margin: 0;">{bpm}</p>
                <p style="color: #666; font-size: 0.8rem; margin: 0;">BPM</p>
            </div>
            """, unsafe_allow_html=True)

        if st.session_state.metronome_running:
            st.markdown("""
            <div style="display: flex; justify-content: center; gap: 8px; margin-top: 1rem;">
                <div style="width: 20px; height: 20px; background: #e94560; border-radius: 50%; animation: beat 0.5s infinite alternate;"></div>
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%; animation: beat 0.5s 0.25s infinite alternate;"></div>
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%; animation: beat 0.5s 0.5s infinite alternate;"></div>
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%; animation: beat 0.5s 0.75s infinite alternate;"></div>
            </div>
            <style>@keyframes beat { from { opacity: 1; transform: scale(1); } to { opacity: 0.3; transform: scale(0.8); } }</style>
            <p style="text-align: center; color: #4ade80; font-size: 0.85rem; margin-top: 0.5rem;">● Running</p>
            """, unsafe_allow_html=True)
        else:
            st.markdown("""
            <div style="display: flex; justify-content: center; gap: 8px; margin-top: 1rem;">
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%;"></div>
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%;"></div>
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%;"></div>
                <div style="width: 20px; height: 20px; background: #333; border-radius: 50%;"></div>
            </div>
            <p style="text-align: center; color: #666; font-size: 0.85rem; margin-top: 0.5rem;">○ Stopped</p>
            """, unsafe_allow_html=True)

        st.markdown("<p style='color: #888; font-size: 0.85rem; margin-top: 1rem;'>Quick Presets:</p>", unsafe_allow_html=True)
        preset_cols = st.columns(4)
        presets = [(60, "Slow"), (100, "Medium"), (140, "Fast"), (180, "Shred")]
        for i, (preset_bpm, label) in enumerate(presets):
            if preset_cols[i].button(f"{label}\n{preset_bpm}", key=f"preset_{preset_bpm}", use_container_width=True):
                st.session_state.metronome_bpm = preset_bpm
                st.rerun()

        st.markdown('</div>', unsafe_allow_html=True)

    st.markdown("<br>", unsafe_allow_html=True)

    tool_col3, tool_col4 = st.columns(2)

    # --- CHORD LIBRARY ---
    with tool_col3:
        st.markdown('<div class="tool-card">', unsafe_allow_html=True)
        st.markdown("### 🎼 Chord Library")
        st.write("Interactive fretboard diagrams for essential chords.")

        chord_type = st.selectbox("Select Chord", list(CHORD_SHAPES.keys()), key="chord_select")
        chord_data = CHORD_SHAPES[chord_type]

        st.markdown(render_fretboard(chord_data["svg_notes"], title=chord_type), unsafe_allow_html=True)

        with st.expander("📋 Text Diagram"):
            st.code(chord_data["ascii"], language=None)

        st.markdown("""
        <div style="background: rgba(15,52,96,0.3); border-radius: 8px; padding: 0.75rem; margin-top: 0.5rem;">
            <p style="color: #a0a0a0; margin: 0; font-size: 0.85rem;">
            <strong style="color: #e94560;">🔴</strong> = Finger position &nbsp;|&nbsp;
            <strong style="color: #888;">✕</strong> = Muted string &nbsp;|&nbsp;
            <strong style="color: #4ade80;">○</strong> = Open string
            </p>
        </div>
        """, unsafe_allow_html=True)
        st.markdown('</div>', unsafe_allow_html=True)

    # --- SONG DATABASE ---
    with tool_col4:
        st.markdown('<div class="tool-card">', unsafe_allow_html=True)
        st.markdown("### 🎵 Song Database")
        st.write("Browse and learn songs. Filter by difficulty and genre.")

        search = st.text_input("🔍 Search songs...", placeholder="e.g. Wonderwall", key="song_search")
        diff_filter = st.multiselect("Difficulty", ["Beginner", "Intermediate", "Advanced"],
                                     default=["Beginner"], key="song_diff_filter")

        filtered = [s for s in SONGS if s["difficulty"] in diff_filter]
        if search:
            filtered = [s for s in filtered if search.lower() in s["title"].lower() or search.lower() in s["artist"].lower()]

        for song in filtered[:5]:
            diff_class = f"badge-{song['difficulty'].lower()}"
            genre_class = f"badge-{song['genre'].lower()}"

            st.markdown(f"""
            <div style="background: #1a1a2e; padding: 0.85rem; border-radius: 10px; margin-bottom: 0.5rem; border: 1px solid rgba(15,52,96,0.3);">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <p style="margin: 0; color: #e94560; font-weight: 600; font-size: 0.95rem;">{song['title']}</p>
                    <span class="badge {diff_class}">{song['difficulty']}</span>
                </div>
                <p style="margin: 0.2rem 0; color: #a0a0a0; font-size: 0.85rem;">{song['artist']}</p>
                <div style="margin-top: 0.4rem;">
                    <span class="badge {genre_class}">{song['genre']}</span>
                    <span style="color: #666; font-size: 0.8rem;">{song['chords']}</span>
                </div>
            </div>
            """, unsafe_allow_html=True)

        if len(filtered) > 5:
            st.caption(f"...and {len(filtered) - 5} more songs")

        st.button("📖 View Full Library", use_container_width=True, key="view_library_btn")
        st.markdown('</div>', unsafe_allow_html=True)

# ============================================
# TAB 3: 12-WEEK COURSE
# ============================================
with tab_lessons:
    st.subheader("📚 12-Week Foundational Course")
    st.caption("The complete path from zero to fretboard freedom.")

    free_count = sum(1 for c in CURRICULUM if c["free"])
    st.markdown(f"""
    <div style="background: rgba(74,222,128,0.08); border-radius: 12px; padding: 1rem; margin-bottom: 1.5rem; border: 1px solid rgba(74,222,128,0.2);">
        <p style="color: #4ade80; margin: 0; font-size: 0.95rem;">
        ✅ <strong>{free_count} weeks are FREE</strong> — Start immediately, no signup required.
        </p>
    </div>
    """, unsafe_allow_html=True)

    for lesson in CURRICULUM:
        status_icon = "🔓" if lesson["free"] else "🔒"
        status_color = "#4ade80" if lesson["free"] else "#e94560"
        card_class = "lesson-card free" if lesson["free"] else "lesson-card"

        st.markdown(f"""
        <div class="{card_class}">
            <div style="display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 0.5rem;">
                <div>
                    <h4 style="color: #e0e0e0; margin: 0; font-size: 1.1rem;">Week {lesson['week']}: {lesson['title']} {status_icon}</h4>
                    <p style="color: #a0a0a0; margin: 0.4rem 0 0 0; font-size: 0.9rem; line-height: 1.5;">{lesson['desc']}</p>
                    <div style="margin-top: 0.6rem;">
                        <span style="color: #888; font-size: 0.8rem; margin-right: 1rem;">⏱️ {lesson['duration']}</span>
                        <span style="color: #888; font-size: 0.8rem;">🎯 {lesson['focus']}</span>
                    </div>
                </div>
                <span style="color: {status_color}; font-size: 0.8rem; font-weight: 700; background: rgba(0,0,0,0.2); padding: 0.3rem 0.8rem; border-radius: 6px; white-space: nowrap;">
                    {'FREE' if lesson['free'] else 'PREMIUM'}
                </span>
            </div>
        </div>
        """, unsafe_allow_html=True)

        if lesson["free"]:
            if st.button(f"▶️ Start Week {lesson['week']}", key=f"start_week_{lesson['week']}", use_container_width=True):
                st.success(f"🎸 Loading Week {lesson['week']}: {lesson['title']}")
                st.info("In the full app, this would load interactive lesson content with video, exercises, and fretboard animations.")
        else:
            if st.button(f"🔒 Unlock Week {lesson['week']}", key=f"unlock_week_{lesson['week']}", use_container_width=True):
                st.info("💡 Ready to level up? The full 12-week course unlocks everything. Subscribe to continue.")

    st.markdown("---")
    st.markdown("""
    <div class="upsell-box">
        <h2 style="color: #e94560; margin-top: 0; font-size: 1.6rem;">🎓 Complete the Foundation</h2>
        <p style="color: #e0e0e0; font-size: 1.1rem; line-height: 1.6; max-width: 600px; margin: 1rem auto;">
            You've experienced the free tools. Now build the skills that last a lifetime.
        </p>
        <p style="color: #a0a0a0; font-size: 1.3rem; font-weight: 700; margin: 1.5rem 0;">
            <strong>12 weeks. 15 minutes a day. Forever changed.</strong>
        </p>
        <div style="display: flex; justify-content: center; gap: 2rem; margin: 1.5rem 0; flex-wrap: wrap;">
            <div style="text-align: center;">
                <p style="color: #e94560; font-size: 1.5rem; font-weight: 700; margin: 0;">$9.99</p>
                <p style="color: #888; font-size: 0.85rem; margin: 0;">/month</p>
            </div>
            <div style="text-align: center;">
                <p style="color: #e94560; font-size: 1.5rem; font-weight: 700; margin: 0;">$79.99</p>
                <p style="color: #888; font-size: 0.85rem; margin: 0;">/year (save 33%)</p>
            </div>
        </div>
        <p style="color: #666; font-size: 0.85rem; margin-top: 1rem;">
            No interruptions. No ads in lessons. Cancel anytime. 30-day money-back guarantee.
        </p>
    </div>
    """, unsafe_allow_html=True)

# ============================================
# TAB 4: SONG LIBRARY
# ============================================
with tab_songs:
    st.subheader("🎵 Song Library")
    st.caption("Learn songs right away. All songs include chord diagrams and strumming patterns.")

    f1, f2, f3 = st.columns(3)
    with f1:
        difficulty_filter = st.multiselect("Difficulty", ["Beginner", "Intermediate", "Advanced"],
                                           default=["Beginner", "Intermediate"], key="full_diff_filter")
    with f2:
        genre_filter = st.multiselect("Genre", ["Rock", "Pop", "Blues", "Folk", "Metal", "Jazz"],
                                      default=["Rock", "Pop", "Folk"], key="full_genre_filter")
    with f3:
        technique_filter = st.multiselect("Technique", ["Strumming", "Fingerpicking", "Power Chords", "Soloing"],
                                          default=["Strumming"], key="full_tech_filter")

    filtered_songs = [s for s in SONGS if s["difficulty"] in difficulty_filter and s["genre"] in genre_filter]
    st.markdown(f"<p style='color: #888; margin-bottom: 1rem;'>Showing {len(filtered_songs)} songs</p>", unsafe_allow_html=True)

    song_cols = st.columns(3)
    for i, song in enumerate(filtered_songs):
        with song_cols[i % 3]:
            diff_class = f"badge-{song['difficulty'].lower()}"
            genre_class = f"badge-{song['genre'].lower()}"

            st.markdown(f"""
            <div class="song-card">
                <h4 style="color: #e94560; margin: 0 0 0.3rem 0; font-size: 1.05rem;">{song['title']}</h4>
                <p style="color: #a0a0a0; margin: 0; font-size: 0.9rem;">{song['artist']}</p>
                <div style="margin: 0.6rem 0;">
                    <span class="badge {diff_class}">{song['difficulty']}</span>
                    <span class="badge {genre_class}">{song['genre']}</span>
                </div>
                <p style="color: #666; font-size: 0.8rem; margin: 0.3rem 0;">{song['chords']}</p>
                <p style="color: #888; font-size: 0.75rem; margin: 0;">{song['chord_count']} chords</p>
            </div>
            """, unsafe_allow_html=True)
            st.button("🎸 Learn", key=f"learn_song_{i}", use_container_width=True)

# ============================================
# TAB 5: PROGRESS TRACKING
# ============================================
with tab_progress:
    st.subheader("📊 Your Progress")
    st.caption("Track your journey. Every minute counts.")

    progress = st.session_state.user_progress

    stat_col1, stat_col2, stat_col3, stat_col4 = st.columns(4)

    with stat_col1:
        st.markdown(f"""
        <div class="metric-card">
            <p style="color: #e94560; font-size: 2rem; font-weight: 700; margin: 0;">{len(progress['lessons_completed'])}</p>
            <p style="color: #a0a0a0; font-size: 0.85rem; margin: 0.3rem 0 0 0;">Lessons Done</p>
        </div>
        """, unsafe_allow_html=True)

    with stat_col2:
        st.markdown(f"""
        <div class="metric-card">
            <p style="color: #e94560; font-size: 2rem; font-weight: 700; margin: 0;">Week {progress['current_lesson'] + 1}</p>
            <p style="color: #a0a0a0; font-size: 0.85rem; margin: 0.3rem 0 0 0;">Current Level</p>
        </div>
        """, unsafe_allow_html=True)

    with stat_col3:
        st.markdown(f"""
        <div class="metric-card">
            <p style="color: #e94560; font-size: 2rem; font-weight: 700; margin: 0;">{progress['streak_days']}</p>
            <p style="color: #a0a0a0; font-size: 0.85rem; margin: 0.3rem 0 0 0;">Day Streak 🔥</p>
        </div>
        """, unsafe_allow_html=True)

    with stat_col4:
        st.markdown(f"""
        <div class="metric-card">
            <p style="color: #e94560; font-size: 2rem; font-weight: 700; margin: 0;">{progress['total_practice_minutes']}</p>
            <p style="color: #a0a0a0; font-size: 0.85rem; margin: 0.3rem 0 0 0;">Minutes Practiced</p>
        </div>
        """, unsafe_allow_html=True)

    st.markdown("---")

    st.subheader("Course Completion")
    completion = (len(progress['lessons_completed']) / 12) * 100
    st.progress(completion / 100, text=f"{completion:.0f}% Complete")

    st.markdown("---")

    st.subheader("Weekly Breakdown")

    for i, lesson in enumerate(CURRICULUM):
        completed = i < len(progress['lessons_completed'])
        status = "✅" if completed else "⬜"
        color = "#4ade80" if completed else "#666"
        bg = "#16213e" if completed else "transparent"
        text_color = "#e0e0e0" if completed else "#666"

        st.markdown(f"""
        <div class="progress-week" style="background: {bg};">
            <span style="color: {color}; margin-right: 1rem; font-size: 1.3rem; width: 24px; text-align: center;">{status}</span>
            <span style="color: {text_color}; font-size: 0.95rem; flex: 1;">
                <strong>Week {lesson['week']}:</strong> {lesson['title']}
            </span>
            <span style="color: {'#4ade80' if lesson['free'] else '#e94560'}; font-size: 0.75rem; font-weight: 600;">
                {'FREE' if lesson['free'] else 'PREMIUM'}
            </span>
        </div>
        """, unsafe_allow_html=True)

    st.markdown("---")

    st.subheader("📝 Log Practice Session")
    log_col1, log_col2 = st.columns(2)
    with log_col1:
        practice_minutes = st.number_input("Minutes practiced", min_value=0, max_value=180, value=15, step=5, key="practice_min")
    with log_col2:
        practice_type = st.selectbox("What did you practice?",
                                     ["Finger exercises", "Chords", "Scales", "Songs", "Technique", "Theory"],
                                     key="practice_type")

    if st.button("✅ Log Session", use_container_width=True, key="log_session"):
        st.session_state.user_progress['total_practice_minutes'] += practice_minutes
        st.session_state.user_progress['streak_days'] += 1
        st.success(f"🎸 Logged {practice_minutes} minutes of {practice_type.lower()}. Keep it up!")
        st.balloons()
        st.rerun()

# ============================================
# FOOTER
# ============================================
st.markdown("---")
st.markdown("""
<div style="text-align: center; color: #555; padding: 2rem 0 1rem 0;">
    <p style="font-size: 1.1rem; color: #888; margin-bottom: 0.3rem;">🎸 <strong style="color: #e94560;">Six String Boot Camp</strong></p>
    <p style="font-size: 0.85rem; color: #666;">Built for guitarists, by guitarists. Free tools forever. Premium course for the committed.</p>
    <p style="font-size: 0.75rem; color: #444; margin-top: 1rem;">© 2026 Six String Boot Camp. All rights reserved.</p>
</div>
""", unsafe_allow_html=True)
'''

with open('app.py', 'w') as f:
    f.write(app_code)

print("✅ app.py created successfully!")
print(f"📄 {len(app_code):,} characters, {app_code.count(chr(10)):,} lines")
